project: Example
database: PostgreSQL

extensions:
  - uuid-ossp

schemas:
  - public
  - staging

exclude:
  dbdocs:
    schemas:
      - staging

dependencies:
  - type: table
    name: core.lookup_values
    refers:
      - core.lookups

seed:
  -

staging:
  -